• Successfully used `cashflow-skill` for logging expenses and `insights-skill` for generating expense and task visualizations (bar, donut, line charts).
• Identified and corrected the Python path for `airbnb-crawl-skill` to `/root/.openclaw/crawl4ai_env/bin/python` after initial issues.
• Faced challenges with agent-to-agent communication due to session visibility restrictions and missing `sessionKey`/`label` for "ven".
• Performed Git operations (stage, commit, push) on `openclaw`, `cashflow-manager`, and `find-your-seat` repositories.
• Noted the importance of specific `sessionKey` or `label` for inter-agent communication via `sessions_send` and the need for `tools.sessions.visibility=all` configuration for broader agent communication.
• Discovered that persistent sub-agents (`thread=true`) are not supported in the current Telegram channel context.